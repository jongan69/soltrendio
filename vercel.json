{
  "crons": [{
    "path": "/api/cron/tweet",
    "schedule": "0 0 * * *"
  }],
  "builds": [{
    "src": "package.json",
    "use": "@vercel/next",
    "config": {
      "installCommand": "yarn && npm install node-webrtc --build-from-source"
    }
  }]
}